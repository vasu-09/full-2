# Copy this file to .env and adjust the IP to match your development machine.
# The Expo client uses the value to talk to the Spring Boot API gateway.
EXPO_PUBLIC_API_URL=http://192.168.1.42:8080

# Optionally override the port that scripts/start-tunnel.js uses when auto-detecting the URL.
# EXPO_PUBLIC_API_PORT=8080